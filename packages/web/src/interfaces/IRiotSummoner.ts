export interface IRiotSummoner {
  id: string,
  name: string,
  accountId: string,
  profileIconId: number,
  revisionDate: number,
  puuid: string,
  summonerLevel: number,
  leaguePoints: number,
  wins: number,
  losses: number,
  rank: string,
  tier: string
}